<section>

    <h2>LES PERFORMANCES</h2>

    <div>
    <?php $perfs = $player_perfManager->findPerf($data["id"]); ?>

    <?php foreach ($perfs as $perf) { ?>

        <div>

            <p><?=$perf["player"] ?></p>
            <p><?=$perf["team"] ?></p>
            <p><?=$perf["points"] ?></p>
            <p><?=$perf["assists"] ?></p>

        </div>

    <?php } ?>
    
    </div>

</section>